const bobaController = require('./bobaController')
const reviewController = require('./reviewController')

// console.log('funnelled both controlled into index')

module.exports = {
  bobaController,
  reviewController
}
